run:
  timeout: 5m

linters:
  enable:
    - goimports
    - govet
    - errcheck
    - staticcheck
    - unused
    - gosimple
    - ineffassign

linters-settings:
  goimports:
    local-prefixes: github.com/douhashi/gh-project-promoter
